# Project Context
> Auto-maintained by AI. You can edit this file freely.
> Last updated: 2025-05-02

## Stack
- TypeScript + Bun (runtime & test)
- Commander.js (CLI framework)
- Chalk (terminal colors)
- AJV (JSON schema validation)
- PowerShell + Bash (installers)

## Architecture Decisions
- CLI installed to `%APPDATA%\opencode\cli\` via .cmd shim in bun bin
- Config deployed to `%APPDATA%\opencode\` (Windows) / `~/.config/opencode/` (Linux/macOS)
- Installer never overwrites existing user config (safe merge)
- LSP servers use custom handler functions for complex installs (Rust, Go, Java, C++, C#, Lua)
- Context preservation is per-project, auto-created by AI

## Conventions
- Commit format: `<type>(<scope>): <description>`
- Version in package.json and install.ps1 `$Version` must stay in sync
- Skills written in Bahasa Indonesia

## Current Status
- [x] Fix Rust LSP installer (auto-install rustup + stable toolchain)
- [x] Fix Lua LSP installer (expanded path detection)
- [x] Auto-context preservation (no manual init needed)
- [x] Auto-prune + auto-archive mechanism

## Important Notes
- install.ps1 `$Version` = "1.5.0" — update both places on release
- Winget exit code 43 = already installed (not an error)
